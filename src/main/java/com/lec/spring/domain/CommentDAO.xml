<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lec.spring.domain.Comment">

<!-- 댓글 조회 -->
	<select id="replyList" parameterType="int" resultType="com.board.domain.CommentDTO">
    	select
        	uid, mb_uid, content, regDate
    	from t_accommo_review
        	where mb_uid = #{mb_uid}        
	</select>

<!-- 댓글 작성 -->
	<insert id="replyWrite" parameterType="com.board.domain.CommentDTO">
    	insert into t_accommo_review(mb_uid, content, regDate)
        	value(#{mb_uid}, #{content}, #{regDate})
	</insert>
    
<!-- 댓글 수정 -->
	<update id="replyModify" parameterType="com.board.domain.CommentDTO">
    	update t_accommo_review set
        	content = #{content}
    	where uid = #{uid}
        	and mb_uid = #{mb_uid}    
	</update>

<!-- 댓글 삭제 -->
	<delete id="replyDelete" parameterType="com.board.domain.CommentDTO">
    	delete from t_accommo_review
    	where uid = #{uid}
        	and mb_uid = ${mb_uid}    
	</delete>

	
</mapper>


















