<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lec.spring.domain.CommentDAO">

	<select id="select" resultType="com.lec.spring.domain.CommentDTO">
		SELECT 
			wr_uid "uid", 
			wr_content content,
			wr_regdate regDate 
		FROM 
			test_write 
		ORDER BY 
			wr_uid DESC	
	</select>

	<select id="selectByUid" resultType="com.lec.spring.domain.CommentDTO">
		SELECT 
			wr_uid "uid", 
			wr_content content, 
			wr_regdate regDate 
		FROM 
			test_write
		WHERE wr_uid = #{uid} 
	</select>
	
	<insert id="insert" flushCache="true"
		parameterType="com.lec.spring.domain.CommentDTO"
		useGeneratedKeys="true"
		keyColumn="wr_uid" keyProperty="uid">
		INSERT INTO test_write
			(wr_subject, wr_content, wr_name)
		VALUES
			(#{subject}, #{content}, #{name})	
	</insert>
	
	
	<delete id="deleteByUid" flushCache="true">
		DELETE FROM test_write WHERE wr_uid = #{uid}
	</delete>
	
	
	
</mapper>


















